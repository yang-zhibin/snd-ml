import pandas as pd

# goal: build train split for neutral hadrons

# read metadata 
# check each energy bin has how many event
# build a balance dataset accroding the files
# add a new column to indicate the train split
